@AGENTS.md

# OriginsMeta — note per Claude

Sito fan non ufficiale su Origins TCG (Koin Games), tre lingue (en/it/es: lo spagnolo dal 25/09/2026; il francese è stato ritirato il 15/09/2026, i testi FR restano nei dati), Next.js 16 + Tailwind 4, deploy su Vercel da GitHub `ultimacenere/originsmeta`. Proprietario: Pierluigi Cella. Contatto pubblico: staff@originsmeta.com.

KB di progetto (stato, decisioni, contatti, log delle sessioni): `G:\Il mio Drive\OriginsMeta\00_KB\_kb_master_originsmeta.md` (documento unico da leggere a inizio sessione; dettaglio storico in `_kb_log_storico.md` nella stessa cartella). A fine sessione aggiornare §1 (Stato) e §10 (Log). Questo file resta la guida tecnica del repo.

## Regole del progetto

- Rispondere e commentare in italiano; i contenuti del sito sono in inglese, italiano e spagnolo e vanno sempre aggiornati insieme, nella stessa sessione (`en` è il tipo di riferimento per i dizionari). **Spagnolo** (richiesta di Pierluigi del 25/09/2026, "terzo asset, rigorosamente seguendo il SEO"): spagnolo neutro con il tú, nomi delle carte in inglese, glossario e posizione dei testi nel codice in `docs/spagnolo.md` (da leggere prima di scrivere contenuti in spagnolo). **Testi di gioco** (25/09/2026, verifica chiesta da Pierluigi "al 100%"): il gioco è tradotto in italiano e spagnolo e traduce le parole chiave, quindi carte, carte create e luoghi usano in IT ed ES il testo o il glossario ufficiale del gioco (Alla rivelazione / Al revelar, Travolgere / Arrollar, spazio / espacio…, `docs/testi-di-gioco.md`), e dallo stesso giorno anche guide, news, storico e interfaccia ("ok allinea anche guide e news"): "la sua abilità Alla rivelazione", mai "il suo On Reveal"; nomi di carte, luoghi e mazzi in inglese (anche "On Reveal Mid Range"). Anche le pastiglie dei tag di World of Origins (`src/lib/keywordLabels.ts`, nomi ufficiali per le parole chiave del gioco, verbi delle carte per le categorie, un test copre ogni tag) e le guide dei mazzi tradotte in automatico (glossario `GAME_KEYWORDS` nel prompt di `deckTranslation.ts`, uguale alle etichette) sono tradotte dal 25/09/2026: in inglese restano solo i testi scritti dagli autori dei mazzi e il gergo ("mazzo move", l'archetipo Move/Combo).
- Niente dati inventati: statistiche, date ed eventi vengono solo dalle fonti ufficiali (pagina Steam, patch notes su Steam, Discord, comunicati). Le stat attuali sono quelle della patch 0.6.3 del playtest (27/08/2026). Eccezione autorizzata da Pierluigi il 15/09/2026: i dati delle carte (testi, statistiche, tag, ID ufficiali) vengono importati dal database community World of Origins (worldoforigins.fun) con `npm run import:woo`, perché la Demo 2.0 non mostra la collezione. Mai importare immagini da altri siti.
- La dicitura "non affiliato a Koin Games" non si toglie mai.
- **Materiale ufficiale Koin** (media kit pubblico + pacco del canale creator, archiviato in `G:\Il mio Drive\OriginsMeta\10_Materiale_Koin\` con checksum e condizioni): Kevin di Koin Games ha autorizzato l'uso il 19/09/2026 con una condizione, in linea con le loro policy — **non si usa per il design del sito**. Quindi sì come contenuto (illustrazioni delle carte, copertine di news e guide, gallerie, screenshot a corredo di un testo, keyart come testata), no come identità o interfaccia (logo o icona Koin come marchio di OriginsMeta o nei dati strutturati, elementi di UI, sfondi, texture, palette, emoji Koin come icone). Regole: mai ritagliare via i crediti impressi (`ILLUS // <illustratore>`, `KOIN GAMES INC | 2026`) né coprirli; mai togliere il watermark "Development Build" dagli screenshot (si tiene, con didascalia), ma si ritagliano le targhette con i nomi dei giocatori; **le carte non ancora annunciate si pubblicano solo se già di dominio pubblico** (fonte citabile: trailer ufficiale, pagina Steam, post Discord o X); i file di lavorazione (`.mkv`, "SFX Only", il PSD) non si toccano; niente ridistribuzione degli originali.
- Ogni news e ogni guida ha SEMPRE una copertina (campo `image`, obbligatorio sulle news): immagine del media kit ufficiale in `public/media/` o miniatura ufficiale YouTube; la home e /news le mostrano (`NewsCover`). Richiesta di Pierluigi del 15/09/2026: senza copertine la home è "un blocco di testo". Dal 19/09 ogni news ha una copertina **diversa** dalle altre.
- **Ogni news è un articolo con pagina propria** (regola di Pierluigi del 21/09/2026: "gli articoli sono firmati editorialmente da me, hanno una pagina propria e i parametri SEO"). Rotta `src/app/[locale]/(site)/news/[slug]/page.tsx`, firmata da `authorOfNews` (default Pierluigi Cella, `src/lib/data/authors.ts`), con dati strutturati NewsArticle (autore = nodo Person della pagina autore) + BreadcrumbList + FAQPage. Per una news nuova si scrivono, in inglese, italiano e spagnolo (`n(en, it, es)`; il quarto argomento, facoltativo, è il vecchio francese): `title` (H1, entro 110 caratteri), `metaTitle` (titolo SERP: con "Origins TCG" dentro entro 60 caratteri, lo controlla `pageTitle`), `description` (120–158 caratteri), `summary` (2–3 frasi: scheda in home e in /news, attacco dell'articolo), `body` in Markdown con sezioni `##` (di solito: In breve, i fatti con i dettagli e le citazioni della fonte, perché conta, cosa non sappiamo ancora), `highlights` (l'elenco "In breve" in cima all'articolo, richiesta di Pierluigi del 21/09: un punto per modifica, ognuno ancorato alla sezione che ne parla; le ancore si scrivono nel Markdown come `## Titolo {#ancora}` o `### Titolo {#ancora}`, `Markdown.tsx` le trasforma in `id`), `faq` facoltative e `updated` quando si aggiorna un articolo già uscito (con un paragrafo "Aggiornamento del …" nel testo). Mai un riassunto unico "tutto di fila": un testo lungo va nel `body`, a sezioni. Link interni nel Markdown sempre con prefisso lingua, anche verso le altre news (`/it/news/<slug>`, `/es/news/<slug>`); le ancore si traducono in ogni lingua e gli `highlights` di ciascuna lingua puntano alle ancore del suo `body`. Le news senza `body` (quelle vecchie) mostrano solo il riassunto nella loro pagina.
- **Quello che si pubblica sul sito arriva da solo sul nostro Discord** (regole di Pierluigi del 24/09/2026), ogni cosa nel suo canale: news in `#announcements` e `#site-news`, patch notes anche in `#metashifting`, guide in `#site-news` (o `#guides`) — lo fa la GitHub Action `.github/workflows/discord-announce.yml` dopo il push su main (`scripts/discord-announce.mjs`, un secret GitHub per canale); mazzi della community in `#community-decks` e tornei pubblici in `#tournaments-feed` — li manda il sito in diretta (`src/lib/community/discordDeck.ts`, `src/lib/tournament/notify.ts`, variabili Vercel `DISCORD_WEBHOOK_DECKS` e `DISCORD_WEBHOOK_URL`). Conta lo slug nuovo: una news corretta non si riannuncia. Una patch nuova va in `#metashifting` solo se la sua news è citata nel campo `news` della patch in `cards.ts`. Tabella completa nel README, sezione "Discord: contenuti automatici". Le novità del sito si raccontano come news vere, con `source: "site"` (serie "Upgrade Meta"): pill OriginsMeta e nessuna "Fonte".
- Nessuna dipendenza nuova senza motivo. Le pagine editoriali restano statiche; la parte community (account, mazzi pubblicati, voti) usa Supabase (`src/lib/supabase/`, `src/lib/community/`, schema in `supabase/schema.sql`) e solo `/account` e le pagine di modifica sono renderizzate sul server; `/decks` e le pagine `/decks/community/[slug]` sono ISR. Le funzioni future (tier list votata, tracker prezzi Steam Market) seguono lo stesso schema: tabella + policy RLS + Server Action, senza rompere la generazione statica.
- Segreti: `.env.local` (non committato) contiene la password del database per `scripts/db-migrate.mjs`; URL e chiave publishable di Supabase sono pubblici e hanno un default in `src/lib/supabase/env.ts`. Mai inserire il Client Secret di Discord o altri segreti nel codice: vanno solo nella dashboard Supabase.
- Prima di ogni push: `npm run build` deve passare senza errori.

## Decisioni di prodotto (note Granola 3.0 e 4.0, 15/09/2026)

- Menu: News · Tier list · Guide · Carte · Mazzi · Deck builder · Tornei ed Eventi. La sezione dei bilanciamenti si chiama **MetaShifting** (rinominata con le note 5.0 del 15/09/2026).
- Home (ordine delle note 7.0 del 16/09/2026, rivisto il 21 e il 22/09/2026): slider a tutta larghezza, calendario (striscia che scorre da sola ma navigabile con frecce, con il logo Origins tra fine e inizio del ciclo), subito le prime tre news "aperte" affiancate con i post-it grandi (il blocco titolo con i tasti Steam/Discord è stato tolto il 22/09/2026 per decisione di Pierluigi: Steam resta nello slider, Discord nel footer; l'H1 resta, unico e `sr-only`), il blocco "Fai la tua mossa" (costruisci, pubblica, organizza), tier list a striscia compatta (mai per prima, con il link alla tier list personalizzabile), MetaShifting a striscia della stessa misura con i tre movers principali (Leggendarie per prime, con la stella), bacheca news (patch note comprese), tre guide per chi inizia, stato del gioco. La sezione "Mazzi" della home è stata tolta (ridondante con le news; decisione di Pierluigi). Ricerca carte sempre visibile nell'header.
- Tier list in tre sezioni (mazzi, leggendarie, carte base); mazzi con tag obbligatori (leggendaria, archetipo, creator); collegamenti incrociati tier list → mazzo → carta → guida. I tre mazzi di esempio del playtest (Swarm, Evil, Discard) sono stati rimossi il 15/09/2026: i mazzi del sito sono solo quelli della community (`decks.ts` resta vuoto per eventuali liste ufficiali).
- Colore dominante degli accenti: menta del logo (i competitor usano viola/rosa). Font: Unbounded (display), Manrope (testo).
- Analytics: un solo modulo, `src/lib/analytics.ts` (catalogo degli eventi in testa, nomi stabili; tabella ed eventi chiave nel README): ogni evento va a Vercel Web Analytics sempre, senza cookie, e a GA4 (`G-9J5Q803XJS`, `GoogleAnalytics.tsx`) solo dopo "Accetta tutto" nel banner cookie, con `ad_storage`/`ad_user_data`/`ad_personalization` su `denied`; ritirare il consenso spegne GA4 e cancella i cookie _ga. I quattro eventi nati il 25/09/2026 (`sign_up`, `deck_created`, `deck_published`, `tierlist_created`) non si rinominano: in GA4 contano per nome. `sign_up` e `login` li manda il browser al ritorno dall'accesso (`?om_auth=` aggiunto da `/auth/callback`, account nuovo riconosciuto da `isNewAccount`). Traffico interno (Ondata 2): `?staff=<codice>` (codice nella KB §11, nel repo solo il suo sha256) o, nelle verifiche con il browser, `localStorage originsmeta.internal.v1 = "1"` insieme al consenso. Più Vercel Speed Insights.
- Deck builder (`/deck-builder`, `src/lib/deckrules.ts`, `src/lib/deckcode.ts`, `src/components/DeckBuilder.tsx`): regole 1 Leggendaria + 12 carte base ×2 = 25 (fonte AMA Koin); formato codice del gioco KGBLDC decifrato (vedi README); campo `key` delle carte da popolare con gli ID ufficiali man mano che vengono appresi; la regola Conquest conta le carte uniche (ogni carta una volta, Leggendaria compresa) con un minimo di 8 fra due mazzi, come la Crimson Cup (annuncio del 24/09/2026; decisione del 25/09/2026, `differentCards` con test in `deckrules.test.ts`). La ricerca del pool (24/09/2026, primo feedback degli utenti) guarda nome, saga e testo della carta, anche in inglese sulle pagine italiane: funzioni pure in `src/lib/cardSearch.ts` con test, condivise con `/cards` (che cerca anche nelle parole chiave di WoO).
- Tasti verso Steam e Discord: sempre `SteamButton`/`DiscordButton` (colori e font ufficiali, richiesta esplicita di Pierluigi). I Discord sono due e non vanno confusi: quello ufficiale del gioco (`officialLinks.discord` in `Footer.tsx`, discord.gg/originstcg, server di Koin Games: footer, guide, eventi) e il nostro (`ORIGINSMETA_DISCORD` in `src/lib/discord.ts`, dal 24/09/2026: loghino `DiscordIconLink` nell'header, tasto nel menu sotto i 640 px, riquadro in fondo a ogni news). Niente elementi bianchi né neri pieni (richiesta del 15/09/2026): le schede sono pannelli blu notte (`card-night`, token `night`/`night-2`/`night-3`) con cornice celeste piena da 3 px (`sky` #3fc4e8, il cristallo del mana; 2 px su chip e cubi data, 6 px sullo slider, niente righette sottili), titoli e nomi in celeste (`text-sky`), testi in grigio chiaro (`pale`/`chalk`, versioni `-muted` per il secondario). `ink` resta solo per il testo scuro sui fondi pieni menta e oro.
- Grafici dei mazzi: `deckStats` + `DeckCharts` (SVG inline, niente librerie); il win rate arriverà dalle API ufficiali.
- Decisioni del 21-22/09/2026 (demo del sito con Pierluigi e Davdas, sistema grafico in `/style`, pagina noindex costruita con le classi vere):
  - Bottone primario `.btn-primary` col gradiente in stile Instagram (`--grad-ig`, testo bianco: unica eccezione alla regola "niente bianco pieno"); scala dei titoli per ruolo `.t-page` / `.t-section` / `.t-item`.
  - Post-it (`Postit`, classi `.postit*`): solo sulle tre news in evidenza della home, grandi, storti e un po' sopra l'angolo della copertina (mai sul soggetto né sui crediti); categoria ricavata dai dati (Nuovo mazzo, Patch, News); `is-fresh` per 72 ore dalla data (sbatte per una decina di secondi, poi ondeggia piano come gli altri; il bollino "Nuovo" pulsa sei volte). Etichette di sezione a penna `.postit-label` (Tier list e MetaShifting in home). Il movimento lento continuo è una scelta di Pierluigi: con "riduci animazioni" tutto fermo.
  - Leggendaria: stella gialla davanti al nome (`.legendary-star`, componente `CardName`), stesso colore e misura delle altre carte; Leggendarie per prime nelle liste (`legendaryFirst`). `.row-spell` segna le magie solo nel deck builder.
  - Carte intere che si girano (`FlipCard`, stili `.card-tile*`): UNA sola carta per il database /cards, la scheda di un mazzo (`DeckCardGrid`, "come le carte nella sezione Carte, più grandi") e /style. Col mouse si gira al passaggio e mostra il testo, il costo in mana resta sempre visibile; su touch niente retro, nome e statistiche sotto la carta e il tocco apre la scheda. Nella scheda di un mazzo il nome sotto la carta c'è solo su touch (col mouse sta sul retro). Il deck builder resta a righe. Anteprima al passaggio del mouse: `CardPeek` (gemma `.deck-peek-mana`).
  - Codici: nell'interfaccia c'è solo il codice del gioco (KGBLDC…, `deckGameCode`), "Copia codice del gioco"; il formato OM1 resta interno (link del deck builder). Deck builder a quattro tasti (Pubblica sul sito, Salva privato con la Server Action `saveDeckPrivate` e stato `draft`, Condividi, Svuota mazzo) con autosalvataggio nel browser.
  - Tier list personalizzabile `/tier-list/create` (`TierListMaker`, codice del link `#TL1…` in `src/lib/tiercode.ts` con test in `npm test`): Leggendarie e carte base, tocco e tastiera, salvataggio nel browser, nessun account. Dal 23/09/2026 il trascinamento col mouse e la penna non usa più il drag and drop nativo ma gli eventi del puntatore (carta che segue il puntatore, fascia evidenziata, scorrimento automatico ai bordi, `data-row` sulle file per `elementFromPoint`) e le non classificate restano agganciate in basso allo schermo da 1024 px; col dito resta il tocco più la barra S A B C D, perché `touch-action` non va toccato.
- **Sezione Tier list, riprogettata il 24/09/2026** (analisi UX con Pierluigi, §1 punto 32 della KB; decisioni: prima della Crimson Cup la pagina mostra stato + anteprime vere, la community si chiama così da 5 liste, la vista dati si chiama "Le più giocate", MetaShifting su /metashifting, schede nell'ordine Mazzi · Leggendarie · Carte):
  - **Tre fonti, tre pagine, una testata**: `/tier-list` (OriginsMeta: fasce solo dai risultati dei tornei, vuote fino alla Crimson Cup; intanto riga di stato e anteprime dei mazzi più votati e delle carte più presenti nei mazzi), `/tier-list/community` (media delle tier list salvate, S=5 … D=1; sotto `COMMUNITY_MIN_LISTS` = 5 liste è un'anteprima dichiarata) e `/tier-list/most-played` (in quanti mazzi pubblicati compare ogni carta, più gli archetipi: popolarità, mai win rate), più il tool `/tier-list/create`. `TierListHeader` le collega con schede sempre visibili e lo stato di ogni fonte (la tendina `TierListNav` non c'è più), più l'indice della pagina fisso sotto l'header; `TierSourceLine` dice fonte, campione e stato. H1 e URL restano distinti per pagina (SEO).
  - **Dati**: `src/lib/tierData.ts` (`loadTierData`) legge una volta mazzi pubblicati e tier list salvate e costruisce le voci (`src/lib/tierTypes.ts`); i numeri stanno in funzioni pure con test (`src/lib/tierstats.ts` + `tierstats.test.ts` in `npm test`: `aggregateLists` media/voti/distribuzione, `usageCounts`, `weightedRating` per ordinare per voto pesato sul numero di voti). Le tre pagine sono in ISR (`revalidate = 300`) come /decks. La vista SQL `tier_card_scores` resta nel database ma il sito non la usa più (non dava la distribuzione). Le fasce di OriginsMeta restano in `src/lib/data/tierlist.ts` (i mazzi non sono più scritti lì: arrivano da Supabase).
  - **Componenti**: `TierExplorer` (client, pre-renderizzato: carte nell'HTML) con `mode` `tiers` (fasce con carte tutte uguali e significato nell'etichetta), `usage` (righe con barra, prime 20 poi "Mostra le altre"), `strip` (anteprima); filtri tipo/costo/allineamento/nome con il numero di carte su ogni valore, vista tabella ordinabile, dettaglio in un `<dialog>` nativo (pannello a destra su desktop, foglio dal basso sotto 640 px) con fascia, media, voti e distribuzione, mazzi e guide; eventi GA4 `tier_entry_open` e `tier_entry_click`. `TierDeckList` per i mazzi. Etichette in `src/lib/tierLabels.ts`, stili `.tier-*` in fondo a `globals.css`. Sul telefono le fasce diventano intestazioni e le carte stanno 4 per riga.
  - **Tool**: da 1024 px fasce a sinistra e non classificate in una colonna a destra, ferma (prima agganciate in basso coprivano la fascia S); sul telefono Salva a tutta riga e gli altri tasti a coppie. Le tier list salvate stanno nella tabella `tier_lists`, **una per utente e per tipo** (indice unico `owner,kind`), si salvano con la Server Action `saveTierList` (`src/lib/community/tierActions.ts`), compaiono in `/account` e nella pagina pubblica dell'utente.
  - **MetaShifting** ha una pagina sua, `/metashifting` (statica, tabella da 768 px e una scheda per modifica sotto); non è nel menu (decisione rimandata da Pierluigi: menu o solo richiamo in home). Su `/tier-list` resta il riquadro `#tracker` con le tre modifiche più grandi dell'ultima patch, per i link già pubblicati.
- **Profilo pubblico `/u/<username>`** (23/09/2026): i mazzi pubblicati e le tier list di un iscritto, in ISR e in sitemap; `/account` resta il pannello privato. **Tetto ai mazzi pubblicati**: 5 per un account della community, nessuno per Influencer, Pro, Staff e admin (trigger `enforce_deck_limit` nello schema + `publishedDeckLimit` in `src/lib/community/queries.ts`).
- **Data e versione del gioco sui mazzi** (23/09/2026): ogni mazzo mostra la data di creazione e la patch in vigore quel giorno, ricavata con `patchAt` da `src/lib/data/cards.ts` (mai dichiarata dall'autore); `/decks` ordina dal più recente e filtra per versione. Dalla diretta Twitch del 23/09/2026 il pannello dei filtri di `/decks` (`DeckExplorer`) si vede sempre, anche con pochi mazzi, e ha anche il filtro per **tipo di autore** (Staff, Pro, Influencer, Community: il tag del profilo).
  - Pop-up dei feedback (`FeedbackWidget`, rotta `/api/feedback`, messaggi nel canale Discord privato dello staff; dal 24/09/2026 con nome o nickname facoltativo, mai pubblicato senza chiedere): misura del rodaggio da spegnere dopo con `NEXT_PUBLIC_FEEDBACK=off`; senza `DISCORD_FEEDBACK_WEBHOOK_URL` il bottone non compare; `TURNSTILE_SECRET_KEY` su Vercel per verificare il CAPTCHA.
  - **"Mandaci la tua guida"** (diretta Twitch del 23/09/2026, decisione di Pierluigi: "modulo nel canale staff"): riquadro in `/guides` → modulo `/guides/submit` (`GuideSubmitForm`, noindex) → rotta `/api/guide-submission` → stesso canale Discord privato dei feedback, con il testo completo in un `.txt` allegato; lo staff la pubblica a mano. Usa lo stesso `DISCORD_FEEDBACK_WEBHOOK_URL` e NON si spegne con `NEXT_PUBLIC_FEEDBACK=off`. Difese comuni alle due rotte in `src/lib/formGuard.ts`. Dettagli nel README. Notifiche Discord dei tornei con `DISCORD_WEBHOOK_URL` (`src/lib/tournament/notify.ts`). Dettagli nel README.
- Tournament Organizer (16/09/2026, decisioni di Pierluigi con le richieste di Davdas): creazione libera per ogni utente loggato, ma solo Creator/Influencer/Pro/Staff (o admin) pubblicano sul calendario (`listed`) e caricano una copertina propria (gli altri scelgono dal media kit); mazzi obbligatori prima dell'avvio (chi non li consegna resta fuori dal tabellone), visibili all'avversario dalla partita e pubblici a torneo finito; screenshot dei referti privati (parti, organizzatore, staff); prima versione a eliminazione diretta con bye a sorte, Conquest o mazzo libero, Bo1/Bo3/Bo5; chat testuale tra i due abbinati per il codice partita (fase 3). Tag unico `OM-XXXX` e link breve `/t/<tag>`. Tornei **pubblici o privati a invito** (`visibility`): il privato è invisibile a chi non è organizzatore, admin, iscritto o invitato (policy `can_view_tournament`), quindi la scheda torneo è dinamica; inviti con link segreto `/t/<tag>/<codice>` (codice in `tournament_secrets`) o per nome utente dalla gestione; mai in calendario. Bot di prova con `node scripts/seed-bots.mjs <TAG> [n]` / `--remove`. Il tabellone parte da solo appena il torneo è al completo con tutti i mazzi consegnati (`tm_autostart`); la data di inizio è solo informativa; le partite del tabellone sono link alla stanza partita (chat, referto, screenshot).
- Community (15/09/2026): accesso senza password (Discord OAuth + magic link via Supabase Auth; dal 20/09/2026 il modulo email è protetto dal CAPTCHA Turnstile, `src/lib/turnstile.ts` + `src/components/Turnstile.tsx`: la verifica del token la fa Supabase, il sito manda solo `captchaToken`; senza `NEXT_PUBLIC_TURNSTILE_SITE_KEY` il widget non compare e l'accesso resta come prima. Etichette del pannello in `src/lib/loginLabels.ts`), pubblicazione dei mazzi dal deck builder con guida obbligatoria (piano di gioco) e sezioni facoltative, voto 1–5 stelle (uno per utente, mai sul proprio mazzo), profilo `/account` con modifica/nascondi/elimina. I testi delle guide sono testo semplice (niente Markdown/HTML) per evitare XSS. Ogni mazzo ha uno o più tipi (`deck_types`: ladder / competitive / fun / tournament, caselle multiple, richiesta di Davdas) scelti da chi pubblica e un archetipo suggerito dalla composizione (`src/lib/archetype.ts`) ma modificabile; ogni profilo ha un tag autore (`badge`: community / influencer / pro / staff) assegnato solo dallo staff, mostrato nella riga dei badge prima di "Community", grande e con scritta nera (mappa colori dettata da Pierluigi il 23/09/2026, supera quella di Davdas: **Staff scritta nera su giallo** `bg-gold text-ink`, **Pro rosso su azzurro** `.badge-pro`, **Influencer gradiente stile Instagram con scritta bianca** `.badge-ig`, **Autore / Author / Autor celeste con scritta scura** `.badge-creator` (id interno `creator`; dal 25/09/2026: chi pubblica contenuti sul sito, assegnato dallo staff, stessi permessi dell'Influencer: niente tetto ai mazzi, tornei in calendario, copertina propria; nato come "Creator" viola, rinominato lo stesso giorno perché troppo vicino a Influencer), Community come prima; tutte da `badgeStyle` in `src/lib/cardArt.ts`); chi è Staff NON mostra il tag "Community"; il tag community di default non si mostra. Scheda mazzo community: Leggendaria in grande accanto al titolo, riassunto in un riquadro, punti di forza in verde (`good`) e punti deboli in rosso (`bad`) su sfondo lavagna (richieste di Davdas del 15/09/2026) con `node scripts/set-badge.mjs <utente> <tag>` (un trigger blocca le modifiche degli utenti). Nel deck builder la pubblicazione mostra un lucchetto a chi non è loggato; builder, link e codici restano liberi. Provider Discord e SMTP di produzione vanno configurati nella dashboard Supabase (vedi README).
- **Soglia di qualità dei mazzi** (25/09/2026, Ondata 2 SEO/GEO): sotto `GUIDE_MIN_WORDS` (75) parole di guida originale (riassunto + sezioni) la scheda è noindex in tutte le lingue e fuori da hreflang, sitemap, ItemList di /decks e schede carta (`indexableLocales`/`deckIndexing` in `src/lib/community/deckQuality.ts`, con test), ma resta navigabile, votabile e in /decks; il modulo di pubblicazione mostra le parole scritte (`guideMeter`). La regola più severa proposta dal verificatore (almeno una sezione, 40 parole per i tag autore) aspetta Pierluigi. Gli "altri mazzi" della scheda sono quelli con la stessa Leggendaria, poi gli altri (`relatedDecks`). I profili /u sono noindex senza mazzi né tier list. Le letture pubbliche di Supabase lanciano sugli errori (`rowsOrThrow` in queries.ts, anche tierlists.ts e le sitemap), così l'ISR tiene la pagina precedente: una build con Supabase irraggiungibile fallisce di proposito; senza rete `NEXT_PUBLIC_COMMUNITY=off npm run build`. JSON-LD di mazzi e profili in `src/lib/jsonld/deck.ts`: autore editoriale = `personId`, iscritti `/#user-<nome>`, carte `cardEntityId`, niente AggregateRating su Article.
- **Guide dei mazzi tradotte in automatico** (richiesta di Pierluigi del 25/09/2026: "i deck degli utenti vanno tradotti"): l'autore scrive la guida in una delle tre lingue (campo "Lingua della guida" del modulo), il sito la traduce nelle altre due dopo la pubblicazione o la modifica. Codice: `src/lib/community/deckTranslation.ts` (puro: impronta del testo `guideHash`, scelta della versione `localizedGuide`, lingue disponibili `guideLocales`, richiesta e controlli della risposta; test in `npm test`), `src/lib/community/translate.ts` (dentro `after()` nelle Server Action, scrive con la sessione del proprietario; `maxDuration = 120` su pubblicazione e modifica), colonna `community_decks.translations` (una traduzione per lingua con l'impronta del testo da cui è fatta; un trigger non sposta `updated_at` quando cambiano solo le traduzioni). Modello `claude-opus-5` con la chiave `ANTHROPIC_API_KEY` (la stessa dell'assistente FAQ): **senza chiave non si traduce nulla** e le pagine mostrano l'originale con la nota. Regole SEO: una sola lingua per pagina (l'originale non si incorpora, si linka la versione nella lingua dell'autore); la versione in una lingua non ancora tradotta è **noindex** ed è fuori da hreflang e sitemap; il nome del mazzo non si traduce. Arretrati e nuovi tentativi: `node scripts/translate-decks.mjs` (`--dry-run`, `--only <slug>`, `--export`/`--from` per traduzioni preparate fuori). Dettagli nel README.

## Struttura

- `src/app/[locale]/…` pagine; il layout radice sta sotto `[locale]` (html lang per lingua). Gruppi di rotte: `(site)` (tutte le pagine, layout con striscia calendario + main) e `(home)` (home con slider prima della striscia). `src/app/global-not-found.tsx` gestisce i 404 fuori dalle lingue.
- `next.config.ts` reindirizza `/` alla lingua del browser (it, es, altrimenti en).
- `src/lib/i18n.ts` locali (`en`, `it`, `es`), `href()`, `alternatesFor()` (con `only` per le pagine che non esistono in tutte le lingue); `src/lib/page.ts` helper `resolveLocale` e `pageMeta` (opzioni `languages` e `noindex`). Dizionari in `src/lib/dictionaries/{en,it,es}.ts`.
- Dati in `src/lib/data/*.ts`, guide in `src/lib/content/guides.ts` (inglese e italiano) e `guides-es.ts` (solo i testi spagnoli, il resto dall'inglese), media in `public/media/`. News sui mazzi pubblicati qui: `source: "community"` (oppure `"staff"` per i mazzi dello staff, che mostrano il solo tag Staff), `url` interno senza lingua e `guides` collegate (componente `NewsLinks`); novità del sito: `source: "site"` (`isSiteNews`); guide ai mazzi della community: `tags.communityDecks` (slug + nome, niente lettura di Supabase), mostrate nella scheda del mazzo come "Guide correlate". Link interni nel Markdown sempre con prefisso lingua.
- Database carte in tre file: `woo-cards.json` (generato, non modificare a mano), `card-lore.ts` (saga, origine EN/IT/ES scritte a mano e testi IT ed ES delle carte, quelli ufficiali del gioco dal 25/09/2026) e `card-history.ts` (storico dalle patch notes ufficiali); `cards.ts` li unisce. Le saghe sono una classificazione nostra.
- **Patch nuove** (procedura del 22/09/2026, prima patch della demo): 1) si aggiunge l'id in `patchOrder` e in `patches` di `cards.ts` (data, link al post Steam, e `label` EN/IT/ES quando la patch non ha numero di versione, come `demo-0921` → "Demo · 21 set"); 2) si trascrivono le modifiche in `card-history.ts` (from/to delle statistiche, `alignment` se cambia, nota EN/IT/ES); 3) finché World of Origins non importa la patch, `cards.ts` applica sopra i suoi dati le modifiche delle patch successive all'import (costo, statistiche, allineamento), quindi schede, deck builder e MetaShifting mostrano già i valori nuovi; quando WoO la importa (`npm run import:woo`) vincono i suoi dati e il testo ufficiale aggiornato; 4) news con le patch notes a sezioni (tabella prima/dopo, "In breve" ancorato), citata nel campo `news` della patch: è così che la GitHub Action la pubblica anche in `#metashifting`; 5) si rilancia `node scripts/llms-txt.mjs` (sezioni Legendaries e Guides di `public/llms.txt`) e, se cambiano le Leggendarie o il numero di carte, si aggiornano le FAQ `legendaries`, `card-list` e `where-cards` (lo dice `faq.test.ts`) e le tabelle delle guide con costo e statistiche (lo dice `guides.test.ts`). Dove mostrare il nome della patch si usa sempre `patchLabel`, mai l'id. Fonti: il post Steam e, quando è più completo, il Discord ufficiale, dichiarando nel testo cosa viene da dove.
- **I Luoghi** (23/09/2026, richiesta di Pierluigi dopo aver visto l'elenco dei competitor): `src/lib/data/locations.ts` (44 luoghi della Demo 2.0 con nome, effetto EN/IT/ES, famiglia di effetti e carte citate), pagina `/locations` con ricerca e filtri (`LocationExplorer`, client component su dati già nell'HTML) e guida `origins-tcg-locations` EN/IT/ES. Nome ed effetti sono trascritti a mano dal database della community World of Origins (i luoghi non stanno in `woo-cards.json` e `import:woo` non li porta); gli effetti in italiano e spagnolo sono nostri ma con il glossario ufficiale del gioco (`docs/testi-di-gioco.md`). **Da fare: confrontarli uno per uno nel gioco**, come per le carte, e poi valorizzare `locationsVerified`.
- **Verifica sul gioco** (22/09/2026): la demo mostra la collezione (My Decks → Cards, "Unowned" acceso per vedere anche le carte non possedute, filtri per mana). Confrontate tutte le 122 carte: statistiche, costi e allineamenti coincidevano; 16 testi di World of Origins erano rimasti indietro (effetti cambiati nelle patch 0.6.2/0.6.3 e formulazioni). Il testo inglese letto nel gioco sta in `card-lore.ts`, campo `en`, e vince su `woo-cards.json`. **Dal 25/09/2026 anche `it` ed `es` sono i testi ufficiali letti nel gioco** (88 italiani e 101 spagnoli su 115 erano diversi dalle nostre traduzioni): trascrizioni in `docs/testi-ufficiali/`, confronto e scrittura con `node scripts/official-texts.mjs <it|es> [--apply]`, procedura e glossario in `docs/testi-di-gioco.md`. Le 22 carte create (token) e le rimosse non stanno nella collezione e non sono verificabili lì: il loro italiano e spagnolo è nostro, con il glossario ufficiale. Rifare la verifica dopo ogni patch, nelle tre lingue.
- Community: `src/app/auth/callback/route.ts` (ritorno OAuth/magic link), `src/proxy.ts` (refresh sessione solo su `/account`, modifica mazzo e pagine dinamiche dei tornei), `src/app/[locale]/{login,account,decks/publish,decks/community/[slug]}`; componenti `LoginPanel`, `AccountMenu`, `PublishDeckForm`, `StarRating`, `OwnerActions`. Tipi delle tabelle in `src/lib/supabase/database.ts` (da aggiornare con lo schema, comprese le `Functions` delle RPC).
- Tournament Organizer (16/09/2026, richiesta del coach e di Davdas): `src/lib/tournament/` (`types.ts`, `util.ts`, `queries.ts`, `actions.ts`, motore puro del tabellone `bracket.ts` con test `npm test`), pagine `src/app/[locale]/(site)/tournaments/{[slug],new,[slug]/deck}` (poi `manage` e `match/[id]`), link breve `src/app/t/[tag]/route.ts`, componenti `TournamentForm`, `JoinTournament`, `TournamentCard`, `TournamentDecksForm`, `TagSearch`, `LocalTime`, `ConfirmButton`. Regole: iscrizioni, mazzi e tabellone si scrivono SOLO tramite le RPC `security definer` di `supabase/schema.sql` (lock sulla riga del torneo, `revoke` da `anon`); file (copertine, screenshot) caricati dal browser nello Storage, mai attraverso le Server Action (limite 1 MB); orari sempre con `LocalTime`; il layout `(site)` non deve leggere Supabase (resta statico): i tornei in calendario arrivano alla striscia da `src/app/api/calendar/route.ts` nel browser. Il builder dedicato (`/tournaments/[slug]/deck`) usa `DeckBuilder` con `preset` e le etichette condivise di `src/lib/builderLabels.ts`. Dettagli nel README, sezione "Tournament Organizer".
- Carte e anteprime: `src/components/FlipCard.tsx` (carta intera che si gira, componente server senza stato; dati da `flipOf` ed etichette da `flipLabels` in `CardChip.tsx`), usata da `CardExplorer` (/cards, piede sempre visibile) e da `DeckCardGrid` (schede dei mazzi, `foot="touch"`); `CardPeek` (anteprima al passaggio del mouse, solo CSS) per chip, righe del builder ed elenco dei mazzi. Post-it: `src/components/Postit.tsx` (freschezza calcolata nel browser). Tier list: `src/app/[locale]/(site)/tier-list/` (OriginsMeta), `/tier-list/community/`, `/tier-list/most-played/`, `/tier-list/create/` (tool), `src/app/[locale]/(site)/metashifting/`, `src/components/{TierListHeader,TierExplorer,TierDecks,TierListMaker}.tsx`, `src/lib/{tierData,tierTypes,tierLabels,tierstats,tiercode}.ts` (+ test), `src/lib/community/tierlists.ts` e `tierActions.ts`. Profilo pubblico: `src/app/[locale]/(site)/u/[username]/`. Feedback: `src/components/FeedbackWidget.tsx`, `src/lib/feedbackLabels.ts`, `src/app/api/feedback/route.ts`, `src/lib/discordWebhook.ts`.
- Nomi di carta nei testi della community: `src/lib/cardlinks.ts` (riconoscimento dei nomi ufficiali, funzione pura `linkCardNames`, solo server: importa il database carte) + `src/components/CardMentions.tsx` (link menta, stili `.card-mention*` in `globals.css`). Usati nella scheda mazzo community (`/decks/community/[slug]`), pronti per i commenti. **Anteprime delle carte fuori dal testo** (25/09/2026, GEO-01: il testo delle carte a metà frase era fino al 64% delle parole di una guida e il 69% di /decks): nell'HTML c'è solo il link col nome, i dati dell'anteprima stanno in `data-peek` sulla prima menzione di ogni carta; `CardMentionEdges` (client component minuscolo, si può montare più volte, tocca solo nodi già idratati) crea il pannello al primo passaggio del mouse o al focus, lo sposta ai bordi della finestra e mette sul link la descrizione in `aria-description` per i lettori di schermo. Stesso schema per `CardPeek` (segnaposto vuoto con `data-peek`; su /decks i dati di una carta ripetuta li ha solo la prima copia, `data-peek-key` / `data-peek-ref`) e per le anteprime del Markdown (regex costruita da `locales`, quindi anche /es/). Parti pure in `src/lib/cardPeek.ts` con test. Senza JavaScript resta il link. Non mettere carte dentro un `<Suspense>` separato senza rivedere il meccanismo.

- **SEO tecnico (Ondata 1 del piano SEO/GEO, 25/09/2026)**: title della home sul marchio con Koin Games, gli hub con un `metaTitle` per la SERP distinto dall'H1 (chiavi nei dizionari, controllate da `src/lib/hubMeta.test.ts`); schede carta, mazzi della community e guide ai mazzi con i modelli di `src/lib/cardTitles.ts` (tipo di carta nel title, Leggendaria davanti al mazzo; in italiano le create sono "carta generata", come la pastiglia) con test su 230 carte × 3 lingue. **Ogni news ha `metaTitle` e `description` in EN/IT/ES obbligatori** (lunghezze, "Origins TCG", link interni verso pagine esistenti, paragrafo "Aggiornamento del …" per chi ha `updated`: `src/lib/data/newsMeta.test.ts`); `events.ts` ha `rules: { news, label }` e `faq.ts` ha `news: { slug, label }[]` (link mostrati in EventCard e in /faq). **Date della sitemap** in `src/lib/lastmod.ts` (solo il giorno, mai nel futuro, mai prima della nascita della lingua): `PAGE_UPDATED` si aggiorna nello stesso commit che cambia il modello di una pagina; `SITE_WIDE_CHANGE` solo per una lingua nuova o un cambio di hreflang, mai per header, footer o ritocchi grafici. Canonical e hreflang li mette ogni pagina con `pageMeta` (il layout non li dichiara più). Robots del layout con `max-image-preview:large`. Indirizzi inesistenti sotto una lingua: `[...rest]/route.ts` risponde 404 con un HTML minimo nella lingua (noindex, niente canonical). Spam `/?r=…&channel=…`: 410 dal proxy. `originsmeta.vercel.app` → 308 su originsmeta.com. `robots.txt` blocca `?_rsc=` e `/xx/login?`: il 25/09/2026 il 78% delle richieste di Googlebot erano prefetch RSC delle griglie di carte (per questo `FlipCard` ha `prefetch={false}`). Lingua dei link `/t` e del ritorno dall'accesso: `src/app/t/locale.ts` (pesi q, ca/gl/eu → es). News correlate: `src/lib/relatedNews.ts` (con test); sulle patch notes il blocco automatico `NewsPatchChanges`; nelle guide "News su questo argomento". Archivio delle carte rimosse in fondo a /cards (`RemovedCardsArchive`).

- **Ondata 2 SEO/GEO (25/09/2026)**. Schede carta: `src/lib/cardPage.ts` (frase d'attacco, In breve, etichette, alt; `legendaryPowers` da riempire solo leggendo la Demo), `src/lib/cardSynergy.ts` (mazzi, co-presenza, catene "genera / generata da", date: `cardPageLastmod` + `cardPageDeckDays` danno lo stesso giorno a `dateModified` e sitemap), `src/lib/community/decksByCard.ts` (una lettura condivisa con le etichette `community-decks` e `community-tier-lists`; errori null in build, rilanciati a sito acceso), `src/lib/jsonld/card.ts` (entità unica per carta, `cardEntityId` unica fonte dell'@id), `src/components/card/*`. Dati strutturati del sito in `src/lib/jsonld/entities.ts` (OriginsMeta, Koin Games, il gioco, WebSite, Person con @id uguali in ogni lingua: `personId`, `memberId`), eventi in `jsonld/events.ts` (campo `ld` di events.ts); `JsonLd.tsx` è solo il componente; etichette in `src/lib/entityLabels.ts` (`KOIN_PERMISSION_PUBLIC` spento finché Pierluigi non decide). Sitemap: `/sitemap.xml` è l'indice di `/sitemap-home.xml` e delle `/<lingua>/sitemap-<sezione>.xml` (`src/lib/sitemapEntries.ts`, dati della community in `src/lib/sitemapData.ts`, da rinnovare con `revalidateSitemaps()` nelle Server Action), feed RSS `/<lingua>/news/feed.xml` (`newsFeed.ts`, etichette in `newsFeedMeta.ts`), IndexNow (`src/lib/indexnow.ts`, `scripts/indexnow.mjs`, job del workflow degli annunci; i mazzi dalla Server Action).

- **Ondata 3 SEO/GEO (25/09/2026)**. Tre guide nuove, pagine primarie della mappa delle query: `origins-tcg-legendaries` (le 11 Leggendarie), `origins-tcg-ranked` (la classificata, anche guida dell'evento `steam-next-fest` in events.ts) e `origins-tcg-conquest` (tre mazzi e carte uniche). `src/lib/content/guides.test.ts` controlla limiti SEO, link e ancore, che la guida alle Leggendarie coincida con il database e che le righe di tabella di tutte le guide (`| [Carta](/xx/cards/slug) | costo | P/S`) abbiano costo e statistiche di oggi. I numeri della community nelle guide classificata e Conquest (19 mazzi, conteggi per Leggendaria, coppie e terne) sono fissati al 25/09/2026 nel testo e nel test: si aggiornano insieme. Dopo una guida nuova si rilancia `node scripts/llms-txt.mjs` (`--check` non scrive). /faq ha 15 risposte in EN/IT/ES (`faq.ts`, ognuna con la sua pagina primaria; `keywords` serve solo all'assistente; test in `faq.test.ts`); `public/llms.txt` è scritto a mano tranne le sezioni Legendaries e Guides, `/llms-full.txt` è la rotta statica di `src/lib/llms.ts` (text/plain, noindex; `llms.test.ts`). /decks ha la sezione "I migliori mazzi di Origins TCG adesso" (#best-decks / #migliori-mazzi / #mejores-mazos; `bestDecks` ed `excludedFromBest` in tierstats.ts) con lo stesso insieme di mazzi di "In breve": i votati con la scheda indicizzabile nella lingua (anche su /tier-list, campo `indexable` di tierData.ts), e l'ordine "Più votati" di `DeckExplorer` usa lo stesso voto pesato (`score`); la riga del metodo nomina gli esclusi che sarebbero in classifica. La home resta statica: l'invito alla tier list della community mostra una frase senza numeri e il conteggio per persone lo scrive il browser (`TierInvite` + `/api/tier-list-counts`, ISR come /api/calendar). /tier-list/community mostra le tier list firmate (Staff, Pro, Influencer, Autore: `signedTierLists`).

- Responsive (16/09/2026): sotto 640 px l'header tiene solo logo, Accedi e Menu (il selettore lingua sta dentro la tendina); i menu a tendina dell'header (Menu, account) usano `AutoCloseDetails`, un `<details>` che si chiude al cambio pagina, al click fuori e con Esc, perché l'header sopravvive alla navigazione lato client. Le griglie di liste con testo `truncate` nei figli hanno sempre `grid-cols-1` come colonna base (senza, il testo non spezzabile allarga la colonna oltre lo schermo: era il caso di /cards e del deck builder); i blocchi affiancati a un'illustrazione hanno una `basis` così scendono sotto sul telefono. Verifica: `document.documentElement.scrollWidth` uguale alla larghezza della finestra a 375 px su ogni pagina.

## Palette ("ink & mint")

Sfondo viola inchiostro (`felt`), carte blu notte (`night`) con cornici celesti piene e titoli celesti (`sky`), testi grigio chiaro (`pale`, `chalk`), accento dominante menta (`mint`) per bottoni e link, giallo citrino solo per leggendarie ed evento evidenziato (`gold`), magenta per nerf e avvisi (`crimson`). Font: Unbounded (display), Manrope (testo), JetBrains Mono (numeri).
